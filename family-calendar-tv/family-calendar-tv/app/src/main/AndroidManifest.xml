<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Only needed if you enable the mic "quick add" button -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Fire TV does not require touchscreen; declare it optional -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/app_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.FamilyCalendarTV"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.FamilyCalendarTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- LAUNCHER: shows in the app grid. LEANBACK_LAUNCHER: shows on
                     the Fire TV home row AND is what lets Alexa launch this app
                     by voice using its label, e.g. "Alexa, open Family Calendar" -->
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MSAL needs a redirect activity for the browser-based sign-in fallback -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="msauth"
                    android:host="com.familycal.tv"
                    android:path="/YOUR_BASE64_SIGNATURE_HASH" />
            </intent-filter>
        </activity>

    </application>
</manifest>
